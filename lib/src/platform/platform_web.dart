/// Detecção de plataforma para dart:html (web)
///
/// Este arquivo é usado quando dart:html está disponível,
/// indicando que estamos executando na web.
library;

/// Indica se a plataforma atual é web
///
/// Retorna `true` porque dart:html só existe na web
const bool platformIsWeb = true;
