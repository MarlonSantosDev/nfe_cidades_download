/// Interface de detecção de plataforma (stub padrão)
///
/// Este arquivo é usado como fallback caso nenhuma das
/// plataformas específicas (io ou html) esteja disponível.
library;

/// Indica se a plataforma atual é web
///
/// Este é o valor padrão (stub). Os arquivos platform_io.dart e
/// platform_web.dart fornecem os valores reais baseados na plataforma.
const bool platformIsWeb = false;
